server:
  port: 8081
  #AWS S3 Bucket properties
aws:
  s3:
    accessKey:
    secretKey:
    region:
    bucket-name:
#MySql DataBase properties
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/image_entity
    username: root
    password: 8610425056aA@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQLDialect